webstreamx is a recompilation of our first demo app it can play quite a few formats
including rtsp streams and as this example shows fly

the uiwebview is disabled here, but you can easily create a simple html and use it here.